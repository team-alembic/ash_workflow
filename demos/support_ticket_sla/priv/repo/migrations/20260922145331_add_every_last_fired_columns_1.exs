defmodule SupportTicketSla.Repo.Migrations.AddEveryLastFiredColumns1 do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  @disable_ddl_transaction true
  @disable_migration_lock true

  def up do
    create index(:tickets, [:state, :backlog_stale_last_fired_at],
             name: "ash_workflow_tickets_state_backlog_stale_last_fired_at_index",
             concurrently: true
           )

    create index(:tickets, [:state, :state_entered_at],
             name: "ash_workflow_tickets_state_state_entered_at_index",
             concurrently: true
           )
  end

  def down do
    drop_if_exists(
      index(:tickets, [:state, :state_entered_at],
        name: "ash_workflow_tickets_state_state_entered_at_index"
      )
    )

    drop_if_exists(
      index(:tickets, [:state, :backlog_stale_last_fired_at],
        name: "ash_workflow_tickets_state_backlog_stale_last_fired_at_index"
      )
    )
  end
end
