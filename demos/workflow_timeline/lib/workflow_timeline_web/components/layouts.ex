defmodule WorkflowTimelineWeb.Layouts do
  @moduledoc """
  Holds the layouts used by the application. See the `layouts` directory.
  """
  use WorkflowTimelineWeb, :html

  embed_templates "layouts/*"
end
